group:
  # .golangci.yaml
  - files:
      - source: .golangci.yaml
        dest: .golangci.yaml
        template:
          user:
            name: 'Maxi'
            handle: '@BetaHuhn'
    repos: |
      bakito/gws
